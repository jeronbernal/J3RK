# =========================================================
# Matchstick LED Macros
# Effects defined in config/leds.cfg
# All macros call STOP_LED_EFFECTS before starting a new
# effect to ensure nothing is left running underneath.
# =========================================================

# Boot — rainbow comet, then settle to standby
[delayed_gcode BOOT_LIGHTS]
initial_duration: 1.0
gcode:
    SET_PIN PIN=chamber_light VALUE=0.20
    SET_PIN PIN=electronics_bay_light VALUE=0.15
    SET_LED_EFFECT EFFECT=ms_boot
    UPDATE_DELAYED_GCODE ID=BOOT_TO_STANDBY DURATION=3

[delayed_gcode BOOT_TO_STANDBY]
gcode:
    MS_STANDBY

# ---------------------------------------------------------
# State macros
# ---------------------------------------------------------

[gcode_macro MS_STANDBY]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_standby

[gcode_macro MS_HOMING]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_homing

[gcode_macro MS_Z_CONTACT]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_z_contact

[gcode_macro MS_BED_PREHEAT]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_bed_preheat

[gcode_macro MS_HOTEND_PREHEAT]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_hotend_preheat

[gcode_macro MS_Z_TILT]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_z_tilt

[gcode_macro MS_BED_MESH]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_bed_mesh

[gcode_macro MS_CLEAN_NOZZLE]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_clean_nozzle

[gcode_macro MS_PRINTING]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_printing

[gcode_macro MS_PRINT_COMPLETE]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_done

[gcode_macro MS_PAUSED]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_paused

[gcode_macro MS_ERROR]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_error

[gcode_macro MS_HEATING_SOAK]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_heating_soak

[gcode_macro MS_FILAMENT_LOADING]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_filament_loading

[gcode_macro MS_FILAMENT_UNLOADING]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_filament_unloading

[gcode_macro MS_FILAMENT_PURGING]
gcode:
    STOP_LED_EFFECTS
    SET_LED_EFFECT EFFECT=ms_filament_purging

[gcode_macro MS_LEDS_OFF]
gcode:
    STOP_LED_EFFECTS
    SET_LED LED=matchstick RED=0 GREEN=0 BLUE=0
